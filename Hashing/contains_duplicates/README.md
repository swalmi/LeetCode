loop on the array and check if the val is in a set
if not, add to the set, else return True (duplicates found)
Complexity: 
time -> o(n) # traverse the array  
space -> worst case O(n) # no duplicates, travers the whole array, no early exit.
Tag: Hash Set
